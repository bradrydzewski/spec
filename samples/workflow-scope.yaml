# system scoped workflow
workflow:
  scope: system
  steps:
    - run: echo system

---

# account scoped workflow
workflow:
  scope: account
  steps:
    - run: echo account

---

# org scoped workflow
workflow:
  scope: org
  org: platform
  steps:
    - run: echo org

---

# project scoped workflow
workflow:
  scope: project
  org: platform
  project: checkout
  steps:
    - run: echo project
